#!/bin/bash

# Owner : Abhay Singh

# Purpose : System Defined Varibales Lab

echo ${SHELL}

echo ${HOME}

echo ${OSTYPE}

echo ${PATH}

echo ${$} # Process id

echo ${PPID} # Parent Process id

#sleep 60

echo ${PWD}
echo ${HOSTNAME}
echo ${UID}
#UID="5000"
#echo ${UID}
sleep 1
echo ${SECONDS}
